# 通用 Python / 深度學習專案規則

## Python 執行規則

- 不准直接使用系統 `python`、`python3`、`pip` 或 `pip3`。
- 所有 Python 指令都必須透過 `uv` 執行，例如 `uv run python ...` 或 `uv run <command>`。
- 所有套件安裝與 lockfile 更新都必須透過 `uv`，例如 `uv add ...`、`uv add --dev ...`、`uv lock` 或 `uv sync`。
- 不准用 `uv pip install` 修改專案依賴；這會繞過 `pyproject.toml` 的 project workflow。
- 執行專案 command 時，必須使用 `uv run <command>`，並明確傳入需要的參數。

## 驗證規則

- 修改 Python 程式後，至少執行 `uv run python -m py_compile <changed-python-files>`。
- 修改 Python 程式後，必須執行 `uv run ruff check .`。
- 修改 Python 程式後，必須執行 `uv run basedpyright`。
- 修改 Python 行為、PyTorch model、Dataset、training loop、資料處理流程或測試後，必須執行 `uv run pytest`。
- 涉及資料處理流程時，必須執行該專案定義的 dry-run、小樣本或 fixture 驗證 command。
- 如果 `basedpyright` 報錯，應先修正型別問題；不要用忽略註解壓掉錯誤，除非已確認是第三方型別定義限制，且在程式旁留下明確原因。
- 若因環境、資料或時間限制無法執行必要驗證，必須在回覆中明確說明未執行的項目與原因。

## Recipe 與 Config 規則

- `src/` library code 不准讀取 YAML，也不准依賴 YAML schema。
- `src/` API 必須接收明確 Python 參數，不得依賴隱性 config default。
- `scripts/` 可以讀取 YAML，負責 experiment orchestration、`extends` merge、config validation，然後呼叫 `src/` API。
- Dataset processing command 不准依賴 YAML。Command 必須接收明確的 dataset id、input path、output artifact path、manifest path，以及通用操作參數。
- Dataset-specific 的 glob pattern、metadata schema、label mapping、feature extraction、windowing、normalization、filtering 等細節，應放在 dataset-specific handler、config、文件範例或測試資料中。
- `cfgs/<experiment_id>/base.yaml` 可以保存可重現的 command 參數組合、artifact path 與 training config；它不是 command 的隱性 default。
- Experiment id、config 目錄、輸出 artifact、manifest 與 run directory 的命名必須一致且可追溯。
- Training scripts 可以引用資料處理 command 產生的 artifact 與 manifest，但不能反過來讓 dataset processing command 依賴 training YAML。

## API 與模型設計規則

- 新增可重用 Python API、PyTorch model、Dataset、training config 時，不要替關鍵參數提供隱性 default。
- 關鍵參數包含 input/output 維度、特徵數、類別數、hidden size、kernel size、dropout、learning rate、batch size、split ratio、random seed 與 artifact path。
- 如果使用者沒有明確提供關鍵參數，程式應 early fail，丟出清楚錯誤訊息，不要猜測或套用看似合理的預設值。
- 簡單 validation 優先 inline 寫清楚，例如 `if value <= 0: raise ValueError(...)`。
- 不要為了一次性檢查新增 helper function，也不要把 `lambda` 指派給變數。
- 共用模型、Dataset、training loop 與 metric code 不准寫死特定 dataset、固定 feature 數、固定 class 數、固定 input length 或固定 label schema。
- Dataset-specific 假設只能放在 dataset-specific handler、factory、config、文件範例或測試資料中。
- CLI 若需要保留既有預設值，必須是使用者已明確要求或相容性需要；新增 CLI 參數預設值前要先確認它不是會影響結果的關鍵設定。
- 命名要反映適用範圍：只能處理特定 dataset 或 experiment 的程式必須在 module、class、function 或文件中標明 scope；真正共用的程式不可依賴特定 dataset schema。

## 英文與命名規則

- 如果使用者提出的英文命名、註解、文件文字不自然、有語法錯誤，或容易造成誤解，必須主動提醒。
- 提醒時要直接給出較自然的英文替代寫法，並簡短說明原因。
- 不要為了迎合使用者而保留明顯錯誤或不自然的英文命名。
- 檔名、module 名稱、class 名稱、function 名稱與 config key 應一致反映 scope、資料來源、任務與輸出用途。

## 文件維護

- 文件入口放在 `docs/index.md`。
- `.rule` 只放通用 agent 操作規則，不放專案細節、資料格式筆記、實驗背景、特定資料集名稱、特定 artifact 名稱或長篇說明。
- `AGENTS.md` 與 `CLAUDE.md` 應以 symbolic link 指向 `.rule`，不要維護重複內容。
- 若需要專案背景、資料格式、實驗流程或模型說明，放在 `docs/`，並從 `docs/index.md` 連結。
